# OpenAI API Key for AI-enhanced feedback (optional)
# If not provided, the app will use deterministic scoring only
OPENAI_API_KEY=your_openai_api_key_here

# Production environment
NODE_ENV=production